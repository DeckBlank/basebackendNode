import dotenv from 'dotenv'
dotenv.config();
export const {
    SECRET,
    API_KEY_EMAIL,
    EMAIL_SIGNIN,
    URL_VALIDATE_EMAIL,
    PORT,
    DB_BDG,KEY,
    MYSQL_USER,
    MYSQL_HOST,
    MYSQL_PASSWORD,
    MYSQL_DATABASE,
    VERSION,
    APP_ENV,
    PROYECTO,
    PAIS,
    SECRET_IMG_SERVICE,
    WEB_DOMAIN,
    INTEGRATOR_ID,
    ACCESS_TOKEN,
    CUENTAS,
    CONTACTO} = process.env;

